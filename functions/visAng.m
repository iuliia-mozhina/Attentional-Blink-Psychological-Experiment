function [pixperdeg, degperpix] = visAng(params)% VISANG convert from degrees visual angle to pixels and vice versa%input:%   params: struct containing %       params.res      resolution of the monitor%       params.sz       size of them monitor in cm %			(widh x height; remember: 1 in = 2.54 cm)%       params.vdist    viewing distance in cm%%calculates the visual angle subtendend by a single pixel%returns the pixel per degree, and the degree per pixel%%z.B. fuer Mueller-Rabbitt%   mr.res=[1280 960];%   mr.sz=[42.6720 32.0040];%   mr.vdist=[50];pix=params.sz./params.res;degperpix=(2*atan(pix./(2*params.vdist))).*(180/pi);pixperdeg=1./degperpix;